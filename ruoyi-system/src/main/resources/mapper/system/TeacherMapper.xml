<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.TeacherMapper">

    <resultMap type="Teacher" id="TeacherResult">
        <id property="id" column="id"/>
        <result property="teacherName" column="teacher_name"/>
        <result property="password" column="password"/>
        <result property="phone" column="phone"/>
        <result property="commissionRate" column="commission_rate"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <sql id="selectTeacherVo">
        select id, teacher_name, password, phone, commission_rate, create_by, create_time, update_by, update_time, remark from edu_teacher
    </sql>

    <select id="selectTeacherById" parameterType="Long" resultMap="TeacherResult">
        <include refid="selectTeacherVo"/>
        where id = #{id}
    </select>

    <select id="selectTeacherByPhone" parameterType="String" resultMap="TeacherResult">
        <include refid="selectTeacherVo"/>
        where phone = #{phone}
    </select>

    <select id="selectTeacherList" parameterType="Teacher" resultMap="TeacherResult">
        <include refid="selectTeacherVo"/>
        <where>
            <if test="teacherName != null and teacherName != ''">
                AND teacher_name like concat('%', #{teacherName}, '%')
            </if>
            <if test="phone != null and phone != ''">
                AND phone = #{phone}
            </if>
            <if test="commissionRate != null">
                AND commission_rate = #{commissionRate}
            </if>
            <if test="params.beginTime != null and params.beginTime != ''">
                and date_format(create_time,'%Y%m%d') &gt;= date_format(#{params.beginTime},'%Y%m%d')
            </if>
            <if test="params.endTime != null and params.endTime != ''">
                and date_format(create_time,'%Y%m%d') &lt;= date_format(#{params.endTime},'%Y%m%d')
            </if>
        </where>
        order by create_time desc
    </select>

    <select id="checkPhoneUnique" parameterType="String" resultType="int">
        select count(1) from edu_teacher where phone = #{phone}
    </select>

    <insert id="insertTeacher" parameterType="Teacher" useGeneratedKeys="true" keyProperty="id">
        insert into edu_teacher (teacher_name, password, phone, commission_rate, create_by, remark, create_time)
        values(#{teacherName}, #{password}, #{phone}, #{commissionRate}, #{createBy}, #{remark}, sysdate())
    </insert>

    <update id="updateTeacher" parameterType="Teacher">
        update edu_teacher set
        teacher_name = #{teacherName},
        password = #{password},
        phone = #{phone},
        commission_rate = #{commissionRate},
        update_by = #{updateBy},
        remark = #{remark},
        update_time = sysdate()
        where id = #{id}
    </update>

    <delete id="deleteTeacherById" parameterType="Long">
        delete from edu_teacher where id = #{id}
    </delete>

    <delete id="deleteTeacherByIds" parameterType="Long">
        delete from edu_teacher where id in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>